import 'package:flutter/material.dart';

void main() {
  runApp(MyAI());
}

class MyAI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("MyAI PRO")),
        body: Center(
          child: Text("APK SUCCESS"),
        ),
      ),
    );
  }
}
